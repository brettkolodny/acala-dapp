import React from 'react';
import {} from ''
import { useApi } from '@acala-dapp/react-hooks';

interface EnumProps {
  type: string;
}

const Enum = ({ type }: EnumProps) => {
  const { api } = useApi();
  return (
  );
};